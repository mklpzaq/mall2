<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.test.mall2.item.service.ItemMapper">
	
	<insert id="insertItem" parameterType="com.test.mall2.item.service.Item">
		INSERT INTO item(
			item_name
			, item_price
		) VALUES (
			#{itemName}
			,#{itemPrice}
		)	
	</insert>
	
	<select id="selectItemList" parameterType="java.util.Map" resultType="com.test.mall2.item.service.Item">
		SELECT 
			item_no
			, category.category_no
			,  category.category_name
			, item_name
			, item_price 
		FROM item join category 
		on item.category_no = category.category_no
		ORDER BY item_no DESC 
		LIMIT #{beginRow},#{pagePerRow}
	</select>
	
	<select id="totalCountItem" resultType="Integer">
		SELECT 
			count(*) 
		FROM item
	</select>
	
	
	
</mapper>


