<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.test.mall2.item.service.ItemMapper">
	
	<insert id="insertItem" parameterType="com.test.mall2.item.service.Item">
		INSERT INTO item(
			item_name
			, item_price
		) VALUES (
			#{itemName}
			,#{itemPrice}
		)	
	</insert>
	
	<select id="selectItemList" parameterType="java.util.Map" resultType="com.test.mall2.item.service.Item">
		SELECT 
			item_no
			, item_name
			, item_price 
		FROM item
		ORDER BY item_no DESC 
		LIMIT #{beginRow},#{pagePerRow}
	</select>
	
	<select id="totalCountItem" resultType="Integer">
		SELECT 
			count(*) 
		FROM item
	</select>
	
	<update id="updateItemForm" parameterType="com.test.mall2.item.service.Item">
		SELECT
			item_name as itemName
			, item_price as itemPrice
		FROM item
		WHERE
			item_no =
			#{itemN0}
	</update>
	
	<update id="updateItem" parameterType="com.test.mall2.item.service.Item">
		UPDATE item
		SET
			item_no = #{itemNo}
			,item_name = #{itemName}
			,item_price = #{itemPrice}
		WHERE
			item_no = #{itemNo}
	</update>
	
	<delete id="deldteItem" parameterType="com.test.mall2.item.service.Item">
		DELETE
		FROM item
		WHERE
			item_no = #{itemNo}		
	</delete>
	
</mapper>


